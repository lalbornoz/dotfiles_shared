#
# Common platform-independent aliases
#

alias cp="cp -i";
alias df="df -h";
alias du="du -sch";
alias j="jobs";
alias jd="jobs -ld";
alias jobs="jobs -l";
alias mv="mv -i";
alias rm="rm -i";

_which gcc && alias gcc="gcc -Wall -std=gnu11 -O0 -g";
_which tmux && alias tmux="env TERM=xterm-256color tmux -2";

if _which hexdump; then
	alias hd="command hexdump -C";
	alias hexdump="command hexdump -C"
elif _which hd; then
	alias hd="command hd -C";
fi;

if _which vim; then
	alias vi=vim vim=vim;
fi;

# vim:filetype=sh foldmethod=marker noexpandtab sw=8 ts=8
